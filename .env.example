# Base URL of the Terminal Job backend API.
VITE_API_BASE_URL=http://localhost:8080/api

# When "true", the UI serves synthetic data locally so it runs without a backend.
# Set to "false" (or remove) once the backend is wired up.
VITE_USE_MOCK=true
